/*
let status = false

    let sigla = String(siglaCurso).toUpperCase()
    let listaAlunos = alunos.aluno
    let nomeCurso = []
    let siglaEspecifica = {}

    listaAlunos.forEach(function(nomeeeCurso){
        listaAlunos = nomeeeCurso.curso
        nomeeeCurso.curso.forEach(function(item){
            if(String(item.sigla).toUpperCase() == sigla){
                status = true
                let ListaAlunos = {}
                ListaAlunos.foto = nomeeeCurso.Foto
                ListaAlunos.nome = nomeeeCurso.Nome
                ListaAlunos.matricula = nomeeeCurso.Matricula
                ListaAlunos.sexo = nomeeeCurso.Sexo
                ListaAlunos.curso = nomeeeCurso.Curso
                ListaAlunos.status = nomeeeCurso.status
                nomeeeCurso.push(ListaAlunos)
            }
        })
    })

    siglaEspecifica.curso = sigla
    siglaEspecifica.aluno = nomeCurso

    if(status == false){
        lista = false
    }

    return lista
}
*/

/************************************************************************************************
Relatório:
 - Dia 1 (27 de novembro): início do projeto. Foram concluidas três funções. O Nicolas ajudou a
    solucionar os problemas existentes.
 - Dia 2 (04 de dezembro): ... !=
************************************************************************************************/